package infovis.data;

/**
 * Loader for bus times in {@code CSV} format.
 * 
 * @author Leo Woerteler
 */
public final class CSVLoader {
  /**
   * Hidden default constructor.
   */
  private CSVLoader() {
    // never used
  }
}
